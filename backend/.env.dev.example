# Скопируйте в корень проекта как `.env.dev` для docker compose
# или задайте те же переменные для локального запуска (см. корневой `.env.dev.example`).

POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_DB=file_changer
POSTGRES_HOST=localhost
PGPORT=5432

REDIS_URL=redis://localhost:6379/0

API_KEY=test-key-dev

PYTHONUNBUFFERED=1
